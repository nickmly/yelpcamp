<% include partials/header %>
    <div class="container">
        <h1>
            <%= campground.name %>
        </h1>
        <p>
            <%= campground.desc %>
        </p>
        <div class="row">
            <div class="col-md-12">
                <div class="thumbnail">
                    <img src="<%= campground.image %>">
                </div>
            </div>
        </div>
    </div>

    <% include partials/footer %>